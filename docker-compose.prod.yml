services:
  app:
    image: krjordan/ryanjordan-portfolio:latest
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
    networks:
      - web

networks:
  web:
    external: true
