services:
  app:
    image: krjordan/ryanjordan-portfolio:latest
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - web

networks:
  web:
    external: true
